{
  "name": "mission-control-ios",
  "version": "1.0.0",
  "description": "Mission Control native iOS shell (Capacitor)",
  "scripts": {
    "setup": "npm install && npx cap add ios",
    "sync": "npx cap sync ios",
    "open": "npx cap open ios",
    "build": "npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/push-notifications": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor/preferences": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
